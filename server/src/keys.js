const fs = require("fs");

module.exports = {
  // OAuth2 Client Credentials
	google: {
		clientID: "googleID",
		clientSecret: "googleSecret"
	},
  // MongoDB connection URI
	mongodb: {
		dbURI: "mongodb://"
	}
};
